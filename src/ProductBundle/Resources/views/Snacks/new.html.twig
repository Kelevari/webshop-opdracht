{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Snacks creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('snacks') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
