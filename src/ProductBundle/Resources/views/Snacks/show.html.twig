{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Snacks</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ entity.price }}</td>
            </tr>
            <tr>
                <th>Special</th>
                <td>{{ entity.special }}</td>
            </tr>
            <tr>
                <th>Special_price</th>
                <td>{{ entity.specialprice }}</td>
            </tr>
            <tr>
                <th>Available</th>
                <td>{{ entity.available }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('snacks') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('snacks_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
